
# GreenMate 프로젝트 개발 지침

## 1. 프로젝트 개요

GreenMate는 MT, OT 등 소규모 단체활동의 계획을 입력받아
환경 영향을 분석하고 현실적으로 실행 가능한 저탄소 대안을
추천하는 AI 기반 행사 설계 서비스다.

## 2. 핵심 개발 원칙

- 기존 프로젝트 구조와 코딩 스타일을 먼저 확인한다.
- 기존에 구현된 기능을 불필요하게 삭제하거나 전면 재작성하지 않는다.
- 새로운 기능을 추가하기 전에 관련 파일과 API를 확인한다.
- 한 번에 하나의 기능을 구현하고 실행 결과를 확인한다.
- 변경 범위를 최소화한다.
- 구현이 완료되면 관련 테스트와 실행 방법을 안내한다.
- 확인하지 않은 파일이나 기능이 존재한다고 가정하지 않는다.
- 불확실한 요구사항은 임의로 확정하지 않고 질문하거나 가정을 명시한다.

## 3. 기술 스택

### Frontend

- React
- TypeScript
- Vite
- Tailwind CSS
- Recharts

### Backend

- Python
- FastAPI
- Pandas
- Pydantic
- Pytest

### AI

- LLM API
- 환경변수로 관리하는 API 인증 키
- 대화형 정보 수집 및 계획 구조화

### Data

- CSV 기반 탄소배출계수 및 비용 카탈로그

## 4. 아키텍처 원칙

- Frontend는 사용자 입력과 결과 시각화를 담당한다.
- Backend는 API, 비즈니스 로직, 데이터 조회를 담당한다.
- Python 계산 모듈은 탄소배출량과 비용을 계산한다.
- 규칙 검증 모듈은 예산, 인원, 거리, 접근성 및 제외 조건을 검증한다.
- LLM은 대화, 정보 추출, 누락 정보 질문, 정성적 설명을 담당한다.
- LLM이 탄소배출량이나 비용을 임의로 생성하지 않도록 한다.
- 수치 계산 결과는 계산 모듈에서 반환한 값을 사용한다.

## 5. 탄소 및 비용 계산 규칙

- 탄소배출량은 등록된 데이터셋의 배출계수를 사용한다.
- 비용은 등록된 참고 비용과 수량을 사용한다.
- 계산 단위와 환산 기준을 명확하게 확인한다.
- 데이터가 없으면 임의의 수치를 만들지 않는다.
- 추정치는 화면에 추정값임을 표시한다.
- 모든 계산은 재현 가능해야 한다.

## 6. AI 응답 규칙

- 사용자의 행사 계획을 구조화된 데이터로 변환한다.
- 필수 정보가 누락되면 필요한 질문을 한다.
- 한 번에 너무 많은 질문을 하지 않는다.
- 사용자 입력을 임의로 변경하지 않는다.
- 분석에 필요한 정보가 충분하지 않으면 분석 실행을 진행하지 않는다.
- 추천 이유는 실제 계산 결과와 검증 결과를 근거로 작성한다.
- AI가 계산한 것처럼 수치를 제시하지 않는다.
- 사용자의 예산, 편의성, 제외 조건을 존중한다.

## 7. 코드 작성 규칙

### Python

- 함수와 변수는 snake_case를 사용한다.
- 클래스는 PascalCase를 사용한다.
- 타입 힌트를 적극적으로 사용한다.
- Pydantic 모델로 API 입력과 출력을 검증한다.
- 계산 로직과 API 라우터를 분리한다.
- 예외 상황을 명확하게 처리한다.

### TypeScript

- 변수와 함수는 camelCase를 사용한다.
- 컴포넌트는 PascalCase를 사용한다.
- API 요청과 응답에 타입을 정의한다.
- 컴포넌트에 계산 로직을 과도하게 넣지 않는다.
- 공통 UI는 재사용 가능한 컴포넌트로 분리한다.

## 8. 데이터 및 보안

- API 키를 소스코드에 직접 작성하지 않는다.
- .env 파일을 Git에 커밋하지 않는다.
- .env.example에는 변수명과 설명만 작성한다.
- CSV 데이터의 출처, 기준연도, 단위, 가정을 기록한다.
- 사용자 입력을 신뢰하지 않고 서버에서 검증한다.

## 9. 작업 방식

기능을 구현하기 전에 다음 순서로 진행한다.

1. 관련 파일 확인
2. 요구사항 확인
3. 변경할 파일과 이유 설명
4. 구현
5. 테스트 또는 실행 확인
6. 변경 내용 요약

## 10. 금지 사항

- API 키 노출
- 근거 없는 탄소배출계수 생성
- 검증되지 않은 추천 결과를 확정적으로 표시
- 기존 기능의 불필요한 삭제
- 관련 없는 파일의 대규모 수정
- 테스트 없이 정상 작동한다고 단정
